{
  "description": "PostgreSQL CREATE statement allows by default",
  "action": "schema.migrate",
  "domains": ["schema"],
  "toolParameters": { "statement_type": "CREATE" },
  "expected": { "status": "ALLOW", "matchedRefs": [] }
}
