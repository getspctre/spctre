---
title: Importing policies
description: Import AGT_YAML policies via the control plane UI or the branch API.
---

Spctre's runtime policy format is **AGT_YAML**. Import is done through the control plane UI (**Policies → Import**), CLI, or branch API.

In the control plane, use **Create policy**, then choose **Upload or paste existing** to upload a file or paste its contents. Spctre records the uploaded filename or a system-generated UI source for provenance.

## Source format

AGT YAML/JSON is imported directly. The import flow also translates a strict, documented subset of Rego and Cedar into AGT rules before normal validation and review. It does not execute Rego or Cedar as a Spctre PDP, and it rejects unsupported or ambiguous native syntax rather than guessing.

| `source_format` value | When to use |
|---|---|
| `AGT_YAML` | Native Spctre rules (default) |
| `OPA_REGO` | Supported deny-only Rego selector rules are converted at import; allow/default decision contracts, unsupported expressions, data lookups, recursion, and custom functions are rejected |
| `CEDAR` | Supported standalone `forbid` statements are converted at import; `permit` relies on Cedar default-deny semantics and is rejected until an equivalent AGT baseline is available |

Cedar action IDs must begin with a connector registered in Spctre's built-in pack catalog (for example, `Action::"github.repo.delete"`). Custom connectors remain available through direct AGT_YAML authoring until native-source connector mapping is configured.

Native imports retain the original text, hash, source dialect, translator version, and source-to-rule mapping with the resulting draft. Action equality maps to Spctre's prefix action matching and is currently marked `LOSSY`; review the preview and explicitly accept it before creating a draft. A native import is not a promise of lossless Rego/Cedar round-tripping after AGT edits. Use `spctre policy import --dry-run` or `spctre policy convert` to preview a conversion without creating a draft.

## Required top-level fields

```yaml
tenant_id: "your-tenant-uuid"
workspace_id: "your-workspace-uuid"
branch_id: "payments-policy"
revision_id: "rev-2026-06-08"
source_format: AGT_YAML
source_hash: "sha256-of-this-file"
rules:
  - stable_rule_id: payments.allow_low_risk_charge
    title: Allow low-risk charge
    effect: ALLOW
    domains: [payments]
    actions: [charge]
    connectors: [stripe]
```

<Callout type="info">
  `source_hash` must be the SHA-256 of the raw source document, not the parsed YAML. The CLI computes this automatically when you run `spctre lint`.
</Callout>

## Optional fields

```yaml
scope: CONNECTOR
environment: production
target_stacks:
  - CREWAI
  - LANGCHAIN
generated_at: "2026-06-08T00:00:00Z"
metadata:
  owner: payments-team
```

## Common import errors

| Error | Cause | Fix |
|---|---|---|
| `source_hash mismatch` | File was modified after hash was computed | Recompute hash with `spctre lint` |
| `unknown stable_rule_id` | Rule ID already exists in another branch | Use a unique ID or import into the existing branch |
| `invalid effect` | Effect value is not one of ALLOW/DENY/WARN/ESCALATE | Correct the value in the source file |
| `missing workspace_id` | Field not present in the document | Add `workspace_id` to the top-level fields |
| `target_stack not supported` | Stack name is not in the supported enum | Check supported values in [Core concepts](/help-docs/developer/getting-started/core-concepts) |

## Linting before import

Always lint a policy file before importing:

```bash
spctre lint my-policy.yaml --strict
```

`--strict` treats WARN-level issues as errors, which is recommended for production policies.

## Automated import (CLI / CI)

For scripts and CI, import a local file with the `spctre policy import` command or the `POST /api/v1/policy/imports` API. Both authenticate with a service key carrying the **`policy:import`** scope.

```bash
spctre policy import my-policy.yaml \
  --connector stripe --scope CONNECTOR \
  --key "$SPCTRE_OPERATOR_TOKEN" --url https://app.spctre.dev
```

```bash
curl -sS -X POST https://app.spctre.dev/api/v1/policy/imports \
  -H "Authorization: Bearer $SPCTRE_OPERATOR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"source":"'"$(cat my-policy.yaml)"'","branchName":"stripe","scope":"CONNECTOR","connector":"stripe"}'
```

<Callout type="warn">
  The `policy:import` scope is **admin-issuable only** (mint a key under **Settings → API Keys**) and is **never** granted to a runtime agent token from `spctre init`. A governed agent therefore cannot import — or approve or publish — its own policy.
</Callout>

Import is **idempotent** on the branch identity `(workspace, scope, environment, connector, branchName)` and the source hash:

| Situation | Result |
|---|---|
| Branch does not exist | New draft branch created (`201`, `created: true`) |
| Branch head already carries this exact source | No write (`200`, `alreadyCurrent: true`) |
| Source changed | New draft revision appended to the same branch (`200`) |

Import **only drafts** a branch/revision — it never approves or publishes, and the currently published bundle is unchanged until someone re-publishes. Review and approve the draft in the control plane (**Policies → Review**), then publish it.
