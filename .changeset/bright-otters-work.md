---
"@spctre/cli": minor
---

Expose the public REST API v1 through `spctre api`, including focused commands
for policy evaluation, reviews, compliance, verification, and operations. Migrate
all CLI and generated-adapter public API traffic to `/api/v1`.
