---
"@spctre/sdk": minor
---

Add `POST /simulations` to the generated client. A pipeline entitled to bulk production simulation can now run the managed replay that publishing requires, instead of collecting every approval and stopping at a gate only the console could satisfy.
